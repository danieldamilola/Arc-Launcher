<UserControl x:Class="Flow.Extensions.IpAddressExtension"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="180" d:DesignWidth="400">
    <Border Padding="{StaticResource Padding3}">
        <StackPanel>
            <!-- Local IP section -->
            <TextBlock Text="Local IP"
                       Foreground="{StaticResource TextSecondary}"
                       FontSize="{StaticResource FontSizeSm}"
                       Margin="0,0,0,4" />
            <Border Background="{StaticResource BgInput}"
                    CornerRadius="{StaticResource RadiusSm}"
                    Padding="12,8"
                    Margin="0,0,0,14">
                <TextBlock x:Name="LocalIpText"
                           Text="Detecting..."
                           Foreground="{StaticResource TextPrimary}"
                           FontFamily="JetBrains Mono, Consolas, monospace"
                           FontSize="{StaticResource FontSizeLg}" />
            </Border>

            <!-- Public IP section -->
            <TextBlock Text="Public IP"
                       Foreground="{StaticResource TextSecondary}"
                       FontSize="{StaticResource FontSizeSm}"
                       Margin="0,0,0,4" />
            <Border Background="{StaticResource BgInput}"
                    CornerRadius="{StaticResource RadiusSm}"
                    Padding="12,8">
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="PublicIpText"
                               Text="Fetching..."
                               Foreground="{StaticResource TextPrimary}"
                               FontFamily="JetBrains Mono, Consolas, monospace"
                               FontSize="{StaticResource FontSizeLg}" />
                    <!-- Loading dots for public IP -->
                    <TextBlock x:Name="LoadingDots"
                               Text=""
                               Foreground="{StaticResource TextSecondary}"
                               FontFamily="JetBrains Mono, Consolas, monospace"
                               FontSize="{StaticResource FontSizeLg}"
                               Margin="4,0,0,0" />
                </StackPanel>
            </Border>

            <!-- Error -->
            <TextBlock x:Name="ErrorLabel"
                       Foreground="{StaticResource Destructive}"
                       FontSize="{StaticResource FontSizeSm}"
                       Margin="0,8,0,0"
                       Visibility="Collapsed"
                       TextWrapping="Wrap" />
        </StackPanel>
    </Border>
</UserControl>
